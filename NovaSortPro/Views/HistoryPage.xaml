<Page
    x:Class="NovaSortPro.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaSortPro.Views"
    xmlns:models="using:NovaSortPro.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16" FlowDirection="{Binding Main.LayoutDirection, Source={StaticResource Locator}, FallbackValue=LeftToRight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="History &amp; Undo Journal" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Undo sorting operations completely. Revert individual file moves, whole sessions, or restore to previous day." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
        </StackPanel>

        <!-- Rapid Actions Panel -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Undo Last Operation" Click="OnUndoLastClick" Style="{StaticResource AccentButtonStyle}" Height="45" HorizontalAlignment="Stretch"/>
                <Button Grid.Column="1" Content="Undo Previous 24h" Click="OnUndoPreviousDayClick" Style="{StaticResource DefaultButtonStyle}" Height="45" HorizontalAlignment="Stretch"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <ComboBox x:Name="SessionComboBox" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession, Mode=TwoWay}" PlaceholderText="Select Session..." Width="200" VerticalAlignment="Center"/>
                    <Button Content="Undo Session" Click="OnUndoSessionClick" Style="{StaticResource DefaultButtonStyle}" Height="38" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Detailed Journal List -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Operation Ledger Journal" FontSize="18" FontWeight="Medium"/>

                <ListView Grid.Row="1" ItemsSource="{Binding JournalEntries}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:UndoRecord">
                            <Grid Padding="8,4" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <FontIcon Grid.Column="0" Glyph="&#xE1E0;" FontFamily="{ThemeResource SymbolThemeFontFamily}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind NewPath}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind OriginalPath, Converter={StaticResource StringFormatConverter}, ConverterParameter='Restores back to: {0}'}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    <TextBlock Text="{x:Bind OperationTime, Converter={StaticResource StringFormatConverter}, ConverterParameter='Operation time: {0}'}" FontSize="10" Foreground="Gray"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="Moved" VerticalAlignment="Center" Foreground="Green" FontWeight="SemiBold"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
