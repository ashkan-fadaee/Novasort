<Page
    x:Class="NovaSortPro.Views.AnalyzerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaSortPro.Views"
    xmlns:models="using:NovaSortPro.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16" FlowDirection="{Binding Main.LayoutDirection, Source={StaticResource Locator}, FallbackValue=LeftToRight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="System Analyzer" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Identify duplicates, deep-clean empty folders, locate massive files, and export system reports offline." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
        </StackPanel>

        <!-- Control Bar -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="PathTextBox" Grid.Column="0" Text="{Binding TargetFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Enter or paste folder path to analyze..." VerticalAlignment="Center" Header="Folder or Drive To Analyze"/>
                <Button Grid.Column="1" Content="Browse..." VerticalAlignment="Bottom" Click="OnBrowseFolderClick" Style="{StaticResource AccentButtonStyle}" Height="38"/>
                <Button Grid.Column="2" Content="Run Deep Analysis" VerticalAlignment="Bottom" Click="OnAnalyzeClick" Style="{StaticResource AccentButtonStyle}" Height="38"/>
            </Grid>
        </Border>

        <!-- Analysis Tabs -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Status Overlay / Progress -->
                <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" FontSize="16" FontWeight="Medium" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>

                <!-- Category Lists TabControl -->
                <Pivot Grid.Row="1">
                    <PivotItem Header="Duplicate Files">
                        <ListView ItemsSource="{Binding Duplicates}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:FileItem">
                                    <Grid ColumnSpacing="12" Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{x:Bind Name}" FontWeight="Medium"/>
                                            <TextBlock Text="{x:Bind FullPath}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                            <TextBlock Text="{x:Bind SHA256}" FontSize="10" Foreground="Gray"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{x:Bind SizeDisplay}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>

                    <PivotItem Header="Large Files">
                        <ListView ItemsSource="{Binding LargeFiles}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:FileItem">
                                    <Grid ColumnSpacing="12" Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{x:Bind Name}" FontWeight="Medium"/>
                                            <TextBlock Text="{x:Bind FullPath}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{x:Bind SizeDisplay}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="DarkRed"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>

                    <PivotItem Header="Empty Folders">
                        <ListView ItemsSource="{Binding EmptyFolders}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Grid Padding="8">
                                        <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>

                    <PivotItem Header="Old Files">
                        <ListView ItemsSource="{Binding OldFiles}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:FileItem">
                                    <Grid ColumnSpacing="12" Padding="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{x:Bind Name}" FontWeight="Medium"/>
                                            <TextBlock Text="{x:Bind FullPath}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{x:Bind DateModified}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>
                </Pivot>

                <!-- Export Controls -->
                <CommandBar Grid.Row="2" DefaultLabelPosition="Right">
                    <AppBarButton Icon="Document" Label="Export CSV Log" Click="OnExportCsvClick"/>
                    <AppBarButton Icon="SaveLocal" Label="Export TXT Log" Click="OnExportTxtClick"/>
                    <AppBarButton Icon="BrowsePhotos" Label="Export HTML/PDF Report" Click="OnExportPdfClick"/>
                </CommandBar>
            </Grid>
        </Border>
    </Grid>
</Page>
