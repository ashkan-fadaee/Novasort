<Page
    x:Class="NovaSortPro.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaSortPro.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16" FlowDirection="{Binding Main.LayoutDirection, Source={StaticResource Locator}, FallbackValue=LeftToRight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="System Settings" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Configure global styles, toggle language preferences, and inspect local storage paths." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
        </StackPanel>

        <!-- Config Options -->
        <ScrollViewer Grid.Row="1" Margin="0,12,0,0">
            <StackPanel Spacing="16" MaxWidth="800" HorizontalAlignment="Left">
                
                <!-- General Section -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="General &amp; Interface" FontSize="18" FontWeight="Medium"/>
                        
                        <!-- Language -->
                        <ComboBox Header="{Binding LanguageLabel}" x:Name="LanguageCombo" SelectionChanged="OnLanguageSelectionChanged" Width="300">
                            <ComboBoxItem Content="English" Tag="en-US"/>
                            <ComboBoxItem Content="فارسی (Persian)" Tag="fa-IR"/>
                        </ComboBox>

                        <!-- Theme -->
                        <ComboBox Header="{Binding ThemeLabel}" x:Name="ThemeCombo" SelectionChanged="OnThemeSelectionChanged" Width="300">
                            <ComboBoxItem Content="Light Theme"/>
                            <ComboBoxItem Content="Dark Theme"/>
                            <ComboBoxItem Content="System Default"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <!-- Data & Security -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Local Storage &amp; Privacy" FontSize="18" FontWeight="Medium"/>
                        
                        <!-- Offline Info -->
                        <TextBlock Text="{Binding OfflineLabel}" Foreground="Green" FontWeight="SemiBold"/>
                        
                        <!-- DB Path -->
                        <TextBox Header="Local SQLite Database" Text="{Binding DatabasePath}" IsReadOnly="True" HorizontalAlignment="Stretch"/>

                        <!-- Log Path -->
                        <TextBox Header="Execution Log File" Text="{Binding LogFilePath}" IsReadOnly="True" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>

                <!-- Developer Footer -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                    <Grid>
                        <StackPanel Spacing="4">
                            <TextBlock Text="NovaSort Pro" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Version 1.0.0 (Release-Ready)" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                            <TextBlock Text="Created by Korosh and Nova" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
