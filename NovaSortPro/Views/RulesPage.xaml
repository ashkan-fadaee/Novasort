<Page
    x:Class="NovaSortPro.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaSortPro.Views"
    xmlns:models="using:NovaSortPro.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16" FlowDirection="{Binding Main.LayoutDirection, Source={StaticResource Locator}, FallbackValue=LeftToRight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Smart Rules" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Set dynamic patterns to customize sorting destination. Active rules map file extensions to organize folders." FontSize="14" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
        </StackPanel>

        <!-- Add Rule panel -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding NewPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Extension (e.g. torrent)..." Header="Extension Pattern"/>
                <TextBox Grid.Column="1" Text="{Binding NewTargetFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Folder Name (e.g. Torrents)..." Header="Target Category Folder"/>
                <Button Grid.Column="2" Content="Add Custom Rule" Click="OnAddRuleClick" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Bottom" Height="38"/>
            </Grid>
        </Border>

        <!-- Rules list -->
        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView Grid.Row="0" ItemsSource="{Binding Rules}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:RuleItem">
                            <Grid Padding="8,4" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Bind Pattern}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind TargetFolder}" VerticalAlignment="Center" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                <CheckBox Grid.Column="2" Content="Active" IsChecked="{x:Bind IsActive, Mode=TwoWay}" VerticalAlignment="Center"/>
                                <Button Grid.Column="3" Content="Delete" Click="OnDeleteRuleClick" Style="{StaticResource DefaultButtonStyle}" Visibility="{x:Bind IsCustom, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Export/Import bar -->
                <CommandBar Grid.Row="1" DefaultLabelPosition="Right">
                    <AppBarButton Icon="Save" Label="Save Rules" Click="OnSaveRulesClick"/>
                    <AppBarButton Icon="Upload" Label="Import Rules (JSON)" Click="OnImportRulesClick"/>
                    <AppBarButton Icon="Download" Label="Export Rules (JSON)" Click="OnExportRulesClick"/>
                </CommandBar>
            </Grid>
        </Border>
    </Grid>
</Page>
