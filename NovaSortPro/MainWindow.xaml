<Window
    x:Class="NovaSortPro.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaSortPro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NovaSort Pro">

    <Grid FlowDirection="{Binding LayoutDirection}" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Navigation shell -->
        <NavigationView x:Name="MainNavView" 
                        Grid.Row="0"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        ItemInvoked="OnNavigationItemInvoked"
                        Header="Smart Organizer"
                        FontFamily="{Binding FontFamily}">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Smart Organizer" Tag="Organizer" Icon="AllApps"/>
                <NavigationViewItem Content="System Analyzer" Tag="Analyzer" Icon="Scan"/>
                <NavigationViewItem Content="Smart Rules" Tag="Rules" Icon="Report"/>
                <NavigationViewItem Content="Bookmarks &amp; Favorites" Tag="Bookmarks" Icon="FavoriteShare"/>
                <NavigationViewItem Content="History &amp; Reversion" Tag="History" Icon="Clock"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <!-- Global Status Bar -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontWeight="Medium" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                <ProgressRing Grid.Column="1" IsActive="{Binding IsBusy}" Width="16" Height="16" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
